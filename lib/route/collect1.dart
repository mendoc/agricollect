import 'package:flutter/material.dart';

void main() => runApp (Collect1());

class Collect1 extends StatelessWidget {
  @override 
  Widget build(BuildContext context) {
    return MaterialApp(
      
    );
  }
}